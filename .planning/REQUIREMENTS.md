# Milestone Requirements

## Validated (Existing Capabilities)

- [x] Read checkpoints by exact name
- [x] Read checkpoints by single-value properties map
- [x] Server-side pagination via limit/offset

## Checkpoint API Enhancements

- [ ] **CHK-01**: Server `GetFlowCheckpoints` and `GetPartitioningCheckpoints` APIs support filtering by multiple values for a given property (e.g. `executionID IN (a, b)`).
- [ ] **CHK-02**: Server backend (`GetFlowCheckpointsIntegrationTests` etc) translates multi-value property filters into valid database `IN` clauses.
- [ ] **CHK-03**: Server correctly applies pagination (`limit` and `offset`) over the combined filtered result set.
- [ ] **CHK-04**: Reader exposes a public method (`getCheckpointsPage`) that combines both `checkpointName` and `checkpointProperties`.
- [ ] **CHK-05**: Reader exposes multi-value property filtering (e.g. `Map[String, Set[String]]`) to match the new server capabilities.

## Future / Defer

- None

## Out of Scope

- Changes to how checkpoints are written/ingested.
- Changes to checkpoint schema beyond the filtering logic.

## Traceability

<!-- Generated by roadmapper -->
